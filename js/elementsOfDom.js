const responseMessage = document.getElementById('result');
const getAllOperators = document.getElementById('formOperators');
const addOtherOperator = document.getElementById('addOperator');
const cleanUpOperator = document.getElementById('cleanOperator');
const deleteOperator = document.getElementById('deleteOperator');
const elemAddition = document.getElementById('addition');
const elemSubtraction = document.getElementById('subtraction');
const elemMultiply = document.getElementById('multiplication');
const elemDivision = document.getElementById('division');